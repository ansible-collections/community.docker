removed_features:
- "docker_image_facts - this alias is on longer availabe, use ``docker_image_info`` instead."
- "docker_image - the ``source`` option is now mandatory."
- "docker_image - the default of the ``build.pull`` option changed to ``false``."
- "docker_image - the ``container_limits``, ``dockerfile``, ``http_timeout``, ``nocache``, ``rm``, ``path``, ``buildargs``, ``pull`` have been removed. Use the corresponding suboptions of ``build`` instead."
- "docker_image - the ``force`` option has been removed. Use the more specific ``force_*`` options instead."
- "docker_image - the ``use_tls`` option has been removed. Use ``tls`` and ``validate_certs`` instead."
- "docker_image - ``state=build`` has been removed. Use ``present`` instead."
- "docker_volume - the ``force`` option has been removed. Use ``recreate`` instead."
- "docker_volume - no longer returns ``ansible_facts``."
- "docker_swarm - ``state=inspect`` has been removed. Use ``docker_swarm_info`` instead."
- "docker_network - the ``ipam_options`` option has been removed. Use ``ipam_config`` instead."
- "docker_network - no longer returns ``ansible_facts``."
- "docker_service - no longer returns ``ansible_facts``."
- "docker_swarm_service - the ``constraints`` option has been removed. Use ``placement.constraints`` instead."
- "docker_swarm_service - the ``limit_cpu`` and ``limit_memory`` options has been removed. Use the corresponding suboptions in ``limits`` instead."
- "docker_swarm_service - the ``log_driver`` and ``log_driver_options`` options has been removed. Use the corresponding suboptions in ``logging`` instead."
- "docker_swarm_service - the ``reserve_cpu`` and ``reserve_memory`` options has been removed. Use the corresponding suboptions in ``reservations`` instead."
- "docker_swarm_service - the ``restart_policy``, ``restart_policy_attempts``, ``restart_policy_delay`` and ``restart_policy_window`` options has been removed. Use the corresponding suboptions in ``restart_config`` instead."
- "docker_swarm_service - the ``update_delay``, ``update_parallelism``, ``update_failure_action``, ``update_monitor``, ``update_max_failure_ratio`` and ``update_order`` options has been removed. Use the corresponding suboptions in ``update_config`` instead."
- "docker_container - the default of ``networks_cli_compatible`` changed to ``true``."
- "docker_container - no longer returns ``ansible_facts``."
- "docker_container - the unused option ``trust_image_content`` has been removed."
